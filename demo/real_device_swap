#!/usr/bin/env bash
set -euo pipefail

SELF_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export LD_LIBRARY_PATH="$SELF_DIR${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
cd "$SELF_DIR"
exec "$SELF_DIR/real_device_swap.bin" "$@"
