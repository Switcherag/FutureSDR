[package]
name = "plugin_api"
version = "0.1.0"
edition = "2021"

[dependencies]
futuresdr = { path = "../..", features = ["plugin"] }
libloading = "0.8"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
