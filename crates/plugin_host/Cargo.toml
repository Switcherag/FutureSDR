[package]
name = "plugin_host"
version = "0.1.0"
edition = "2021"

[dependencies]
plugin_api = { path = "../plugin_api" }
futuresdr = { workspace = true }
libloading = "0.8"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
