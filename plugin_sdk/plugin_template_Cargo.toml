[package]
name = "my_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
plugin_api = { path = "<path-to-futuresdr>/crates/plugin_api" }
futuresdr = { path = "<path-to-futuresdr>", features = ["plugin"] }
